[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "memlite"
version = "1.0.1"
description = "Lean single-file SQLite + sqlite-vec semantic memory for AI agents (no vector server)."
requires-python = ">=3.10"
dependencies = [
    "sqlite-vec>=0.1.6",
    "openai>=1.0",
]

[tool.setuptools.packages.find]
include = ["memlite*"]
